{
  "name": "server",
  "version": "1.0.0",
  "description": "Server",
  "main": "index.js",
  "scripts": {
    "market-server": "json-server --watch ./database/market-data.json --port2090",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "The man who sold the world",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
