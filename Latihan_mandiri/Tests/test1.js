function century(year) {
  let split = `${year}`.split('')
  let join = slit.forEach((num,index)=>{
    if(index == 3){
      return ,num
    }
  })
  return;
}

console.log(century(1901))
